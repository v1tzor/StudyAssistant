CREATE TABLE calendarSettingsEntity (
  id INTEGER PRIMARY KEY NOT NULL,
  number_of_week TEXT NOT NULL,
  week_schedule_view_type TEXT NOT NULL
);

INSERT INTO calendarSettingsEntity (id, number_of_week, week_schedule_view_type)
VALUES (1, 'ONE', 'COMMON');

fetchSettings:
SELECT *
FROM calendarSettingsEntity;

updateSettings:
INSERT OR REPLACE INTO calendarSettingsEntity (id, number_of_week, week_schedule_view_type)
VALUES ?;